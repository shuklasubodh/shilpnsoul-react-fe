# Leave unset locally to use Vite's proxy to http://localhost:3000.
# Set this to the deployed backend API origin in production.
VITE_API_BASE_URL=https://shilpnsoul-backend-admin.vercel.app/api
VITE_LIVE_MODE=false
VITE_GUEST_CHECKOUT_ENABLED=true
VITE_REGISTRATION_ENABLED=false
